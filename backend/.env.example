## SkyAgent backend environment (example)
#
# Copy to `backend/.env` and fill values.
# DO NOT commit real keys.

# ── Data source API keys ────────────────────────────────────────────────────

# FlightAware AeroAPI v4
FLIGHTAWARE_API_KEY=

# CheckWX METAR/TAF API
CHECKWX_API_KEY=

# OpenSky OAuth2 Client Credentials (client_id / client_secret)
OPENSKY_USER=
OPENSKY_PASSWORD=

# ── Server config ───────────────────────────────────────────────────────────

# Comma-separated list of allowed CORS origins
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:4200

# ── Model config ────────────────────────────────────────────────────────────

# Optional: path to a trained model weights file
MODEL_WEIGHTS_PATH=

# Scale factor applied to raw (untrained) model output to convert to minutes
STGNN_OUTPUT_SCALE=100

# ── vLLM / LLM config (Phase 3 — Hopper) ───────────────────────────────────

# Point to the GPU node running vLLM (get hostname from SLURM job log)
# Example: http://g001:8000/v1  (or http://localhost:8000/v1 via SSH tunnel)
VLLM_BASE_URL=http://localhost:8000/v1

# API key set in the vLLM server SLURM script (--api-key flag)
VLLM_API_KEY=skyagent-dev

# Model name served by vLLM
VLLM_MODEL=Qwen/Qwen3.5-9B-Instruct

# ── Dev / test ──────────────────────────────────────────────────────────────

# Set to 1 to load responses from fixtures/ instead of live APIs
USE_FIXTURES=0
